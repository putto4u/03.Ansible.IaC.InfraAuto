YAML(YAML Ain't Markup Language)은 사람이 읽기 쉽도록 설계된 데이터 직렬화 형식으로, 설정 파일(Configuration File) 작성에 주로 사용됩니다. CI/CD 파이프라인(예: GitHub Actions, GitLab CI)에서 YAML을 사용하는 경우가 많습니다.

YAML 작성의 **핵심 규칙**은 **구조, 들여쓰기, 데이터 타입 표현** 세 가지입니다.

-----

##  YAML 작성의 기본 규칙

YAML은 세 가지 기본 데이터 구조(스칼라, 매핑, 시퀀스)를 **공백(Space)을 사용한 들여쓰기**로 구분합니다.

### 1\. 들여쓰기 (Indentation) 규칙

  * **공백 사용:** YAML에서 **가장 중요한 규칙**은 논리적인 구조를 나타내기 위해 반드시 \*\*공백(Space)\*\*만 사용해야 한다는 것입니다.
  * **탭(Tab) 사용 금지:** **탭(Tab)은 절대 사용해서는 안 됩니다.** 대부분의 YAML 파서는 탭을 오류로 처리합니다.
  * **일관성 유지:** 계층을 나타내는 들여쓰기 깊이는 보통 2칸 또는 4칸의 공백을 사용하며, 파일 전체에서 **일관성**을 유지해야 합니다.

### 2\. 매핑 (Mapping, Dictionary/Object)

**'키-값' 쌍**으로 데이터를 표현하며, 콜론($:$) 뒤에 공백 하나를 넣고 값을 작성합니다.

```yaml
# 예시: 사용자 설정
user:
  name: Alice # 'name'이 키, 'Alice'가 값
  age: 30     # 'age'가 키, 30이 값
  role: developer
```

### 3\. 시퀀스 (Sequence, Array/List)

순서가 있는 **목록**을 표현하며, 각 항목은 **하이픈($-$)과 공백 하나**로 시작해야 합니다.

```yaml
# 예시: 기술 스택 목록
skills:
  - Python
  - JavaScript
  - SQL
```

### 4\. 스칼라 (Scalar, Value)

실제 데이터 값(문자열, 숫자, 불리언 등)을 의미합니다.

  * **문자열:** 일반적으로 따옴표 없이 사용합니다. 값이 공백이나 특수 문자로 시작하는 경우, \*\*작은따옴표(' ')\*\*나 \*\*큰따옴표(" ")\*\*를 사용합니다.
  * **숫자/불리언:** 따옴표 없이 작성하면 자동으로 숫자나 불리언(`true`/`false`)으로 인식됩니다.

<!-- end list -->

```yaml
is_active: true # 불리언
version: 3.11   # 숫자
path: /usr/local/bin # 따옴표 없이 문자열
message: "Hello: World!" # 특수문자(:)가 포함된 경우 큰따옴표 사용
```

-----

##  추가적인 고급 규칙

### 5\. 다중 라인 문자열 표현

긴 문자열을 여러 줄로 표현할 때 사용합니다.

  * **리터럴 블록($|$):** 줄 바꿈 문자($\mathbf{\backslash n}$)를 포함하여 **작성된 그대로** 유지합니다.
  * **접힘 블록($>$):** 줄 바꿈 문자를 공백으로 대체하여 하나의 긴 문자열로 처리합니다.

<!-- end list -->

```yaml
literal_text: |
  첫째 줄
  둘째 줄은
  줄 바꿈이 유지됩니다.

folded_text: >
  긴 텍스트를
  여러 줄로 작성해도
  한 줄의 공백으로 처리됩니다.
```

### 6\. 주석 (Comments)

  * **샵($\#$):** 설명을 추가할 때 사용하며, 샵 기호($\#$) 뒤에 공백 하나를 두고 주석 내용을 작성합니다.

<!-- end list -->

```yaml
# 이 줄은 주석입니다. 파서에 의해 무시됩니다.
setting: value # 인라인 주석도 가능합니다.
```

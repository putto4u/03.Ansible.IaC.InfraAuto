Jinja2는 Python 기반의 \*\*템플릿 엔진(Template Engine)\*\*입니다. 이는 로직(데이터)과 프레젠테이션(디자인/구조)을 분리하여, 동적인 콘텐츠를 생성할 때 사용되는 강력하고 유연한 도구입니다.

Ansible의 IaC(Infrastructure as Code)에서 Jinja2는 \*\*설정 파일(Configuration File)\*\*이나 **스크립트**를 동적으로 생성하는 데 필수적으로 사용됩니다.

-----

## 1\. Jinja2의 핵심 역할 및 특징 ✨

### 1.1. 로직과 프레젠테이션의 분리

  * **템플릿(Template)**: `.j2` 확장자를 가지며, 데이터의 구조와 형식을 정의합니다. 여기에는 변수나 제어 구조(반복문, 조건문)를 위한 \*\*플레이스홀더(Placeholder)\*\*만 포함됩니다.
  * **데이터(Data)**: 템플릿에 채워 넣을 실제 값(Ansible의 경우 **변수**).
  * **결과**: Jinja2가 템플릿과 데이터를 결합하여 최종적으로 완성된 텍스트 파일(HTML, XML, 설정 파일 등)을 생성합니다.

### 1.2. Ansible에서의 활용 (IaC)

Ansible은 Jinja2를 기본 템플릿 엔진으로 사용하며, 인프라 자동화에서 다음과 같은 핵심적인 역할을 합니다.

1.  **동적 설정 파일 생성**: 서버의 IP 주소, 포트 번호, 사용자 이름 등 **서버마다 다른 값**을 템플릿에 넣어 동일한 형식의 설정 파일을 대량으로 생성합니다.
2.  **조건부 설정**: 서버의 OS 버전이나 환경(개발/운영)에 따라 설정 파일 내용 일부를 변경하는 조건문 로직을 구현합니다.

-----

## 2\. Jinja2 문법의 기본 요소 📜

Jinja2는 템플릿 내에서 데이터를 표시하고 로직을 제어하는 세 가지 주요 구분자(Delimiter)를 사용합니다.

| 구분자 | 유형 | 설명 | 예시 |
| :--- | :--- | :--- | :--- |
| **`{{ ... }}`** | **변수(Variable)** | 변수의 값을 출력합니다. Ansible에서는 인벤토리 변수, 팩트(Facts) 등을 참조합니다. | `웹 서버 포트는 {{ http_port }} 입니다.` |
| **`{% ... %}`** | **제어 구조(Control Structure)** | `if/elif/else`와 같은 조건문이나 `for`와 같은 반복문을 정의합니다. 출력되지는 않습니다. | `{% if env == 'prod' %} ... {% endif %}` |
| **`{# ... #}`** | **주석(Comment)** | 템플릿 엔진이 무시하는 주석입니다. 최종 출력 파일에는 나타나지 않습니다. | `{# 이 부분은 운영팀 설정을 위한 것임 #}` |

-----

## 3\. 실무 예제: Nginx 가상 호스트 설정 파일 생성

Ansible에서 `template` 모듈을 사용해 Nginx의 가상 호스트(Virtual Host) 설정 파일을 생성하는 예시입니다.

### 3.1. Jinja2 템플릿 파일 (`nginx_vhost.conf.j2`)

이 템플릿은 서버마다 다른 `server_name`과 `document_root`를 변수로 받아 동적으로 설정 파일을 생성합니다.

```nginx
server {
    listen 80;
    
    # 템플릿 변수: 서버 도메인 이름을 동적으로 삽입
    server_name {{ vhost_domain }};

    # 템플릿 변수: 웹 파일 경로를 동적으로 삽입
    root {{ doc_root }};
    index index.html;

    # 제어 구조: 환경이 'prod'일 때만 HTTPS 리다이렉션 설정 추가
    {% if env == 'prod' %}
    # 강제 HTTPS 리다이렉트 로직
    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    }
    {% endif %}

    # 기타 로그 및 에러 처리 생략...
}
```

### 3.2. Ansible 플레이북 (`tasks`)

Ansible `template` 모듈을 사용하여 템플릿 파일을 서버에 배포합니다.

```yaml
- name: Generate and deploy Nginx Vhost configuration
  ansible.builtin.template:
    src: nginx_vhost.conf.j2       # 소스 Jinja2 템플릿 파일
    dest: /etc/nginx/sites-available/{{ vhost_domain }}.conf # 대상 서버에 최종 저장될 경로
  
  # 플레이북 내 vars 섹션에서 변수 전달 예시
  vars:
    vhost_domain: myapp.example.com
    doc_root: /var/www/myapp/public
    env: prod
  
  notify: restart nginx # 설정 파일 변경 시 Nginx 재시작
```

이 방식을 통해 수많은 서버에 대해 IP나 도메인만 다르고 구조는 동일한 설정 파일을 **단 하나의 템플릿**으로 관리할 수 있게 되며, 이것이 곧 **IaC의 일관성 및 재사용성**을 확보하는 핵심 기술입니다.

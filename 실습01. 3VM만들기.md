VM 환경에 쿠버네티스 클러스터를 구축하고 애플리케이션을 배포하기 위해 필요한 **Terraform 설정(네트워크/방화벽), Kubernetes Secret 파일**을 포함한 전체 예시
이 예시에서는 **VirtualBox**를 로컬 가상화 도구로 가정하며, 모든 IP, ID, 비밀번호는 임시로 생성되었습니다.

-----

##  1단계: 인프라 구축 준비 및 초기화 (Terraform)

### 1-1. Terraform 작업 디렉터리 및 파일 구성

프로젝트 폴더를 만들고, `main.tf`, `variables.tf`, `setup.sh` 파일을 생성합니다.

### 1-2. `variables.tf` (변수 정의)

민감 정보 및 환경 설정을 변수로 정의합니다.

```terraform
# variables.tf

variable "ssh_user" {
  description = "VM 접속 사용자 이름"
  default     = "k8sadmin"
}

variable "ssh_password" {
  description = "VM 접속 비밀번호"
  default     = "MySecureP@ss123" # 임시 비밀번호
}

variable "mysql_root_password" {
  description = "MySQL Root 비밀번호"
  default     = "DBPassw0rd456" # 임시 비밀번호
}
```

### 1-3. `setup.sh` (K8s 설치 스크립트)

Terraform이 VM 생성 후 SSH로 접속하여 실행할 리눅스 셸 스크립트입니다. Docker, Kubeadm 등을 설치하고 방화벽(UFW) 설정을 해제하는 역할을 합니다.

```bash
#!/bin/bash
# setup.sh: VM 내부에서 실행될 K8s 설치 스크립트 (간략화)

# 1. 방화벽 비활성화 (개발 환경 단순화를 위해)
sudo ufw disable

# 2. Docker 설치
sudo apt-get update
sudo apt-get install -y docker.io
sudo systemctl start docker
sudo systemctl enable docker

# 3. 쿠버네티스 도구 설치 (kubeadm, kubelet, kubectl)
sudo apt-get update && sudo apt-get install -y apt-transport-https curl
curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
echo "deb http://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list
sudo apt-get update
sudo apt-get install -y kubelet kubeadm kubectl
sudo apt-mark hold kubelet kubeadm kubectl

# 4. SWAP 끄기 (K8s 필수 요구 사항)
sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab
```

### 1-4. `main.tf` (인프라 및 K8s 구성)

VM 3개 생성, 네트워크 설정, 그리고 K8s 설치 스크립트(`setup.sh`)를 실행합니다.

```terraform
# main.tf: VirtualBox VM 생성 및 K8s 환경 준비

provider "virtualbox" {
  # VirtualBox Provider 설정 (사전에 설치 필요)
}

# VM 네트워크 (호스트 전용) 설정 (임시 IP 대역)
resource "virtualbox_hostonly_if" "vboxnet" {
  name    = "vboxnet0"
  ip      = "192.168.56.1"
  netmask = "255.255.255.0"
  dhcp    = false
}

# 1. K8s 노드 VM 3개 정의
resource "virtualbox_vm" "k8s_node" {
  count       = 3
  name        = "k8s-node-${count.index}"
  # ... (OS 이미지, CPU, RAM 설정 코드 생략)

  # VM SSH 접속 및 사용자 설정
  guest_os_type = "Ubuntu_64"
  # (실제 환경에서는 box_name, disk 설정 등 복잡한 설정이 추가됨)

  network_adapter {
    host_only_interface = virtualbox_hostonly_if.vboxnet.name
  }

  # 2. K8s 설치 스크립트 실행 (setup.sh)
  provisioner "remote-exec" {
    inline = [
      "echo ${var.ssh_password} | sudo -S bash -s < /dev/stdin",
    ]
    script = file("setup.sh") # setup.sh 파일 실행

    connection {
      type     = "ssh"
      user     = var.ssh_user
      password = var.ssh_password
      host     = "192.168.56.${count.index + 101}" # 임시 IP 할당: 101, 102, 103
    }
  }
}
```

### 1-5. 초기화 및 실행 명령 (필수)

작업 디렉터리에서 다음 명령을 실행하여 Terraform 환경을 준비해야 합니다.

```bash
terraform init
# VirtualBox Provider 및 플러그인을 다운로드 및 초기화합니다.
```

-----

##  2단계: 애플리케이션 배포 설정 (Kubernetes YAML)

VM 3개 위에 쿠버네티스 클러스터가 성공적으로 구성되었다고 가정하고, 애플리케이션을 배포하기 위한 YAML 파일을 작성합니다.

### 2-1. `mysql-secret.yaml` (비밀번호 보안)

민감 정보인 MySQL ROOT 비밀번호를 **Base64로 인코딩**하여 Secret 리소스로 정의합니다.

> **참고:** `bXktc3Ryb25nLXJvb3QtcGFzc3dvcmQ=`는 변수 파일에 정의한 임시 비밀번호 (`DBPassw0rd456`)를 Base64 인코딩한 값입니다.

```yaml
# mysql-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque # 일반적인 키-값 형식의 비밀 데이터
data:
  # MySQL ROOT 비밀번호 (Base64 인코딩 값)
  password: QkJJUGFzc3dwMHJkNDU2 # DBPassw0rd456 인코딩 값
```

### 2-2. `services-deployment.yaml` (배포 정의)

1단계에서 작성했던 Deployment 및 Service 정의에, **DB 영속성**을 위한 **PVC(Persistent Volume Claim)** 정의를 추가합니다.

```yaml
# services-deployment.yaml: 배포 및 영속성 정의

# (Nginx Deployment 및 Service는 이전 답변과 동일하여 생략)
# ...

---
# 1. MySQL 데이터 영속성 요청 (PVC)
apiVersion: v1
kind: PersistentVolumeClaim # 영속 볼륨 요청
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce # 하나의 노드에서만 읽기/쓰기 가능
  resources:
    requests:
      storage: 5Gi # 5GB 스토리지 요청

---
# 2. MySQL DB 서버 (Deployment)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-db-deployment
spec:
  # ... (생략)
  template:
    spec:
      containers:
      - name: mysql-container
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret # 이전에 정의한 Secret 사용
              key: password # Secret의 'password' 키 사용
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql # 컨테이너 내 DB 저장 경로

      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pvc # PVC와 연결
```

###  애플리케이션 배포 

  * **Secret 활용:** MySQL 비밀번호를 \*\*`mysql-secret.yaml`\*\*로 분리하여 **하드 코딩**을 방지하고, Deployment가 이 Secret을 참조하여 환경 변수를 설정하도록 했습니다.
  * **PVC 사용:** `PersistentVolumeClaim (PVC)`을 정의하여, MySQL 컨테이너가 재시작되더라도 DB 데이터가 영구적으로 보존될 수 있도록 **영속적인 스토리지**를 요청했습니다. (실제 환경에서는 이 PVC를 충족시켜 줄 StorageClass 및 PV가 필요합니다.)

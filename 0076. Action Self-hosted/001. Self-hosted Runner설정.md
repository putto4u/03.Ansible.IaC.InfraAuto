## GitHub Self-hosted Runner의 이해와 구축

GitHub Actions에서 기본으로 제공하는 가상 서버(GitHub-hosted runner) 대신, 사용자가 직접 관리하는 서버(AWS EC2, 온프레미스 서버 등)를 **Self-hosted runner**로 등록하여 워크플로우를 실행할 수 있습니다.

---

### 1. Self-hosted Runner를 사용하는 이유

* **커스텀 사양**: 기본 러너보다 더 높은 CPU, 메모리, GPU 사양이 필요할 때 유용합니다.
* **로컬 네트워크 접근**: 프라이빗 서브넷 안에 있는 AWS 리소스(RDS, 내부 API 등)에 접근해야 할 때 사용합니다.
* **비용 절감**: 장시간 빌드가 필요한 프로젝트의 경우, GitHub의 분당 과금 대신 기존에 운영 중인 서버를 활용하여 비용을 아낄 수 있습니다.
* **소프트웨어 제어**: 빌드 환경에 특수한 OS나 대용량 도구가 미리 설치되어 있어야 하는 경우 유리합니다.

---

### 2. AWS EC2를 활용한 러너 구축 단계

#### 1단계: GitHub에서 등록 정보 확인

1. 저장소의 **Settings > Actions > Runners** 메뉴로 이동합니다.
2. **New self-hosted runner** 버튼을 클릭합니다.
3. 러너를 설치할 OS(예: Linux)와 아키텍처(예: x64)를 선택합니다.
4. 화면에 나타나는 **Download** 및 **Configure** 명령어를 복사해 둡니다.

#### 2단계: EC2 서버 설정 및 설치

EC2 인스턴스에 접속하여 위에서 복사한 명령어를 실행합니다.

```bash
# 폴더 생성 및 이동
mkdir actions-runner && cd actions-runner

# 러너 패키지 다운로드 (GitHub 제공 주소 사용)
curl -o actions-runner-linux-x64-2.311.0.tar.gz -L https://github.com/actions/runner/...

# 압축 해제
tar xzf ./actions-runner-linux-x64-2.311.0.tar.gz

# 설정 스크립트 실행 (토큰 포함)
./config.sh --url https://github.com/사용자명/저장소명 --token 아젠다_토큰_값

# 러너 실행
./run.sh

```

#### 3단계: 워크플로우 YAML 수정

워크플로우가 이 서버에서 돌아가도록 `runs-on` 항목을 변경합니다.

```yaml
jobs:
  build:
    runs-on: self-hosted  # 기본값 또는 설정한 Label 이름

```

---

### 3. AWS 환경 서비스 및 과금 주의사항

Self-hosted runner를 운영할 때 발생하는 **유료 서비스** 항목입니다.

* **EC2 인스턴스 비용 (유료)**: 러너가 구동되는 동안 **EC2 인스턴스 유형별 시간당 요금**이 발생합니다. (예: t3.medium 사용 시 시간당 약 $0.0416)
* **EBS 스토리지 비용 (유료)**: 소스 코드와 빌드 결과물이 저장되는 **EBS 볼륨 비용(GB당 월 약 $0.08)**이 발생합니다.
* **NAT Gateway (유료 주의)**: 만약 EC2를 프라이빗 서브넷에 두고 GitHub와 통신하게 한다면, **NAT Gateway 처리 비용($0.045/GB)** 및 **시간당 유지비**가 발생합니다.
* **데이터 전송 (Data Transfer Out)**: 빌드된 결과물을 외부(Docker Hub, 다른 리전 등)로 보낼 때 인터넷 데이터 전송 요금이 부과됩니다.

---

### 4. 보안 주의사항 (중요)

* **퍼블릭 저장소 주의**: 퍼블릭 저장소에 Self-hosted runner를 연결하면, 악의적인 사용자가 Pull Request를 통해 해당 서버에서 임의의 코드를 실행할 위험이 있습니다. **프라이빗 저장소에서만 사용을 권장**합니다.

---

Next Step: Self-hosted Runner 서비스 등록(자동 시작) 방법

GitHub Self-hosted runner의 개념과 AWS 상에서의 구축 방법을 정리했습니다. 과금의 핵심인 EC2와 NAT Gateway 비용을 명시하여 교재로서의 실무적 관점을 유지했습니다.

본문 밖의 메시지는 이 아래에만 기록합니다. 요청하신 대로 'Next Step' 형식과 과금 안내를 포함하여 작성했습니다. 추가적인 설정(예: 시스템 서비스 등록)이 필요하시면 말씀해 주세요.

### 멀티 레포지토리 기반의 GitOps 배포 체계 구축

본 실습은 인프라 관리용 저장소(`web_dep`)와 개발팀 소스 저장소(`web_dev`)를 분리하여 운영하는 현대적인 배포 모델을 다룹니다. 이를 통해 인프라 설정과 서비스 콘텐츠를 독립적으로 관리하고 백업할 수 있습니다.

---

## 1. 멀티 레포지토리 디렉토리 구조

실습에 사용되는 두 개의 저장소는 각각 독립적인 목적을 가지며 다음과 같이 구성됩니다.

```text
[인프라 관리 저장소: web_dep]             [개발팀 소스 저장소: web_dev]
~/projects/web_dep/                     (GitHub 전용 또는 별도 폴더)
├── .gitignore                          ├── index.html
├── web_dep.yml (내 PC 실행용)            ├── css/
├── deploy_logic.yml (GitHub 로직)       │   └── style.css
├── setup/                              ├── js/
│   └── install_engine.sh               │   └── main.js
└── nginx/                              └── images/
    └── default.conf                        └── logo.png

```

---

## 2. [내 PC] 프로젝트 초기화 및 보안 설정

관리자 PC의 `web_dep` 디렉토리에서 작업을 시작하며, 민감한 정보가 외부로 유출되지 않도록 설정합니다.

```bash
# 1. 작업 디렉토리 생성 및 이동
mkdir -p ~/projects/web_dep
cd ~/projects/web_dep

# 2. Git 저장소 초기화
git init

# 3. 보안을 위한 .gitignore 작성
cat <<EOT > .gitignore
inventory.ini
*.pem
*.key
.vault_pass
EOT

# 4. 설정 반영
git add .gitignore
git commit -m "Initial commit: Set infra project environment"

```

---

## 3. [내 PC → 대상 서버] SSH 신뢰 관계 구축

Ansible이 10대의 서버에 동시 접속할 수 있도록 공개 키를 배포합니다. 이 작업은 **내 PC**에서 수행합니다.

```bash
# 1. SSH 키 쌍 생성 (기존 키가 없을 경우)
ssh-keygen -t rsa -b 4096 -f ~/.ssh/my-key.pem -N ""

# 2. 대상 서버(101~110번)로 공개 키 전송
# 예제 IP: 192.168.10.101 ~ 110
for i in {101..110}; do
  ssh-copy-id -i ~/.ssh/my-key.pem.pub ubuntu@192.168.10.$i
done

```

---

## 4. [GitHub] 중앙 집중형 통합 배포 로직 (`web_dep`)

`web_dep` 저장소에 올라갈 `deploy_logic.yml`은 선언적(Declarative) 방식을 유지하기 위해 쉘 스크립트 대신 Ansible 모듈로 Nginx를 설치하도록 구성합니다.

**`deploy_logic.yml` (GitHub `web_dep` 저장소 업로드)**

```yaml
---
- name: 멀티 레포지토리 기반 통합 배포 파이프라인
  hosts: webservers
  become: true
  tasks:
    - name: 1. Nginx 설치 (IaC 선언적 방식)
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: 2. 인프라 저장소(web_dep) 임시 클론
      git:
        repo: "https://github.com/사용자계정/web_dep.git"
        dest: "/tmp/infra_setup"
        version: main
        force: yes

    - name: 3. 커스텀 엔진 설정 파일 적용 (인프라 저장소 파일 사용)
      copy:
        src: "/tmp/infra_setup/nginx/default.conf"
        dest: "/etc/nginx/sites-available/default"
        remote_src: yes
        owner: root
        group: root
        mode: '0644'
      notify: Reload Nginx

    - name: 4. 개발팀 저장소(web_dev) 소스 배포
      git:
        repo: "https://github.com/사용자계정/web_dev.git"
        dest: "/var/www/html"
        version: main
        force: yes

    - name: 5. 임시 디렉토리 정리
      file:
        path: "/tmp/infra_setup"
        state: absent

  handlers:
    - name: Reload Nginx
      service:
        name: nginx
        state: reloaded

```

---

## 5. [내 PC] 배포 실행 및 결과 확인

내 PC의 `web_dep.yml`은 GitHub에 최종 커밋된 `deploy_logic.yml`을 참조하여 실행됩니다.

**`web_dep.yml` (내 PC 보관)**

```yaml
---
- name: GitHub 최신 커밋본 기반 통합 배포 트리거
  import_playbook: deploy_logic.yml

```

**실행 명령**

```bash
ansible-playbook -i inventory.ini web_dep.yml

```

---

## 6. AWS 관련 비용 및 보안 주의사항

* **AWS EC2 인스턴스 종료**: 10대의 서버(`101~110`) 실습 시, **Free Tier** 무료 제공 시간은 약 **3일(75시간)**이면 소진됩니다. 실습 직후에는 반드시 인스턴스를 **종료(Terminate)** 하십시오.
* **네트워크 트래픽**: 서버 10대가 두 개의 저장소에서 데이터를 가져오므로 총 20회의 Git 호출이 발생합니다. 대용량 파일 배포 시 대역폭 사용량에 유의해야 합니다.
* **보안 그룹**: 인스턴스 보안 그룹에서 **80(HTTP)** 포트와 **22(SSH)** 포트가 내 PC의 IP에 대해 허용되어 있는지 확인하세요.

---

Next Step: Private 레포지토리 접근을 위한 Deploy Key 등록 및 Ansible 연동 실습

---

Next Step: Deploy Key 설정 및 연동 실습
